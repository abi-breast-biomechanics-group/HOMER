from HOMER.mesher import Mesh



class DepNode:
    # a dependent node takes points, and then gives associations with the underlying points data
    def __init__(self, mesh, source_elem, source_elem_coords):
        return

    def eval():
        return
        


class DepMesh(Mesh):
    def __init__(self, base_mesh, depNodes, depNodeInds=None):

        #set the nodes of the base mesh as not optimisable
        
        #get the locations in the mesh true param array.

        
    
    def generate_params(self):
        """
        For all dependent nodes, generates their associated params, and updates the true mesh param array.
        """
            

    def generate_mesh(self):
        super().generate_mesh()

        #then 


        self.evaluate_embeddings = 
