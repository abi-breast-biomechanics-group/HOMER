[project]
name = "HOMER"
version = "0.2.2"
dependencies = [
    "jax",
    "scipy",
    "numpy",
    "pyvista",
    "sparsejac",
    "tqdm",
]

[project.optional-dependencies]
docs = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "mkdocstrings[python]>=0.25",
    "mkdocs-autorefs>=1.0",
]
